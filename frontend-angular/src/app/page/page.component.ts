import { Component } from '@angular/core';
//import { MENU_ITEMS } from './pages-menu';

@Component({
  selector: 'app-pages',
  template: `
  <p>App Pages works!</p>
  `,
})
export class PageComponent {
  //menu = MENU_ITEMS;
}
